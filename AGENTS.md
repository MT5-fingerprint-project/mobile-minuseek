# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

> ⚠️ Reste en **SDK 54**. L'app cible Expo Go, et l'Expo Go publié sur l'App Store /
> Play Store plafonne à SDK 54 (les builds Expo Go pour SDK 55/56 ne sont pas
> distribués sur les stores). Monter de SDK casserait le chargement dans Expo Go.
> Pour aller au-delà du SDK 54, il faudrait passer à un development build.
