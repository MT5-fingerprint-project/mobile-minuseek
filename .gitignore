# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
# NB : `expo-env.d.ts` est volontairement versionné (il ne l'est pas dans le
# template Expo). Rien ne le régénère sur un clone propre — ni `expo export`, ni
# une commande `typegen` en SDK 54 — donc sans lui `tsc --noEmit` échoue en CI.
# Son contenu est stable (une directive `/// <reference>`).

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android

.claude

.env

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli